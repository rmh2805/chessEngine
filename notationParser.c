#include "notationParser.h"


// [Source Rank, Source File, Dest Rank, Dest File]
char* notationToMove (board_t board, const char * move) {
    if(board == NULL || move == NULL)
        return NULL;

    char* toReturn = calloc(4, sizeof(char));

    return NULL;
}
